#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <pigpio.h>
#include "constants.h"

void waitSignal(){
    // while(1){
        // if (gpioRead(PORT_LED_RX) == 1) {
            // printf("Detected");
            // sleep(SLEEP_TIME);
    // }
    // sleep(SLEEP_TIME/100.0f);
    
	// }
}
